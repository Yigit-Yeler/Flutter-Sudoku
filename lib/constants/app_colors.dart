import 'package:flutter/material.dart';

class AppColors {
  // Base Colors
  static const Color lightBackground = Colors.white;
  static const Color darkBackground = Color(0xFF121212);

  // Difficulty Colors
  static const Color easyColor = Colors.green;
  static const Color mediumColor = Colors.orange;
  static const Color hardColor = Colors.red;

  // Theme Colors
  static const Color natureBase = Colors.green;
  static const Color oceanBase = Colors.blue;

  // Shadow Colors
  static const Color shadowColor = Colors.black12;
  static const Color lightShadow = Color(0x1A000000);
  static const Color darkShadow = Color(0x1AFFFFFF);

  // Opacity values for backgrounds
  static const double backgroundOpacity = 0.1;
  static const double selectedOpacity = 0.3;
  static const double errorOpacity = 0.2;
}
